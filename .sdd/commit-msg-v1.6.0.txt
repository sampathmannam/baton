v1.6.0: Tier 0+1+design rules+DPDPA, red-dot visual identity fixes

Tier 0 (cleanup): Glance widget, QS tile, share-receiver, in-app voice
stop, download progress, storage size. Tier 1 (survival): encrypted
vault backup (Argon2id + AES-GCM), BIP39 12-word recovery phrase
with FLAG_SECURE, 3-step onboarding, FTS4 search, theme switcher,
plain export, undo controller. Tier 3 subset (privacy): vault mode
(Visible/Hidden) with PIN gate, recovery phrase screen, threat-model
copy.

v1.6.0 design-rule enforcement (audit v3.0 §4.6): 5 new static-scan
tests guard against the 8 design rules (no overdue/missed language,
no "what's new" modal, no "feature of the day" tips tab, no sync
indicator, no magic-string self-destruct timers).

v1.6.0 DPDPA obligations (audit v3.0 §4.7): in-app data summary on
Person Detail, one-tap erasure path (primary action, not buried
in Settings), parental-consent gate on the capture sheet.

v1.6.0 visual identity (red-dot critique §3): hold-to-reveal recovery
phrase replaces the static 12-word grid, signature accent colour
(`BatonAccentDot`/`Line`/`Bar`/`LeftTag`) at 2dp on key surfaces,
typography weight-500 at empty-state headlines, 5 hand-drawn
empty-state illustrations tinted to the accent, vault-mode subtle
indicator (2dp accent dot in the Today top bar when in Hidden mode).

Cuts applied (audit v3.0 §3.1): §2.13 reach-out status pill (RSD
research + Pendo 80%-never-used; non-shaming variant was still a chip
on other people), §3.1 VeraCrypt-style hidden volume (Czeskis 2008
tattle attack — replaced with the v1.5.0 behavioural model),
§3.4 P2P sync, §3.5 speaker ID, §3.6 per-field encryption
(all deferred to v2.1+ as opt-in), §4.5 Rewind cut permanently.

Tests: 307 baseline -> 479 on HEAD (+172 new across 4 tiers + 8
design-rule + 3 hold-to-reveal), 0 failures, 7 skipped (same as
v1.5.7 baseline). Database schema v10 -> v13 (3 migrations
renumbered sequentially). 8 modified files in the working tree
fixed during the 4-way merge (TodayScreen, SettingsViewModel,
DatabaseModule, MainActivity, SettingsSheet, strings.xml,
InstructionEntity, InstructionDao).

Build: assembleRelease GREEN. app/build/outputs/apk/release/app-release.apk
~24 MB.
